Steps to build the code
./build.sh

This will generate the build files in _build directory
also the executable will be generated there named: LoggerLibraryExecutable
To run the binary use the below command
./_build/LoggerLibraryExecutable

For FileSink log file will be generated at: /tmp/

Input is taken from config.txt file, present in the project directory
ts_format:ddmmyyyy
log_level:WARN
sink_type:CONSOLE               ---- CONSOLE/FILE/DB
dbhost:127.0.0.1                ---- will be picked if sink_type is DB
dbport:3306                     ---- will be picked if sink_type is DB
file_location:/tmp/logFile.log  ---- will be picked if sink_type is FILE
